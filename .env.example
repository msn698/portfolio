# Contact form (existing)
RESEND_API_KEY=
FROM_EMAIL=
OWNER_EMAIL=
NEXT_PUBLIC_WHATSAPP_NUMBER=

# Admin portal — set real values in Vercel only, never commit them.
# ADMIN_PASSWORD_HASH: generate with `node scripts/hash-admin-password.mjs`
# ADMIN_SESSION_SECRET: generate with `openssl rand -base64 48` (min 32 chars)
ADMIN_EMAIL=
ADMIN_PASSWORD_HASH=
ADMIN_SESSION_SECRET=
