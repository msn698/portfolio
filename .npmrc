# @react-three/drei@10 / fiber@9 declare react@^19 peers while the app is on
# React 18; the lockfile is built with legacy peer resolution. Remove this
# once the app upgrades to React 19.
legacy-peer-deps=true
